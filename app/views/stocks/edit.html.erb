<h2>Edit Stock</h2>

<%= render partial: 'errors' if @stock.errors.any? %>

<%= form_for([@stock_exchange, @stock]) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>

  <%= f.submit 'Edit Stock' %>
<% end %>